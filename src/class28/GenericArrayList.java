package class28;

import java.util.ArrayList;

public class GenericArrayList {
    public static void main(String[] args) {

        ArrayList<String> name = new ArrayList<>();
        name.add("Umut");

        //All classes are a child class of Object class in Java.

        Object a = new String("some value");

        //Generic arrays are the arrays that we do not specify the data type. Now no one uses the arrays like this.
    }
}
