package class12;

import java.util.Locale;

public class StringDemo2 {
    public static void main(String[] args) {

        String str = "JavA is FUN 1235 &+^/"; //Only converts letters
        System.out.println(str.toLowerCase()); //Converting upper case to lower case
        System.out.println(str.toUpperCase()); //Converting lower case to upper case

    }
}
